services:
  - type: web
    name: panchayat-backend
    env: node
    branch: main
    rootDir: backend 
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: "5000"
      - key: MONGO_URI
